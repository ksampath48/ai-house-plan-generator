{
  "name": "ai-house-plan-generator",
  "version": "1.0.0",
  "private": true,
  "description": "AI-powered conceptual house planner with static frontend and Node API",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check api/generate-plan.js && node --check lib/generatePlan.js"
  },
  "engines": {
    "node": ">=18"
  }
}
